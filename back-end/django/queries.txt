QUERY: Create 3 new users.

User.objects.create(first_name="Yuzuha", last_name="Shibata", email_address = "yuzuha48@gmail.com", age=23)
User.objects.create(first_name="Jackson", last_name="Muehlbauer", email_address="jmuehl@umn.edu",  age=24)
User.objects.create(first_name="Sophia", last_name="Sasaki", email_address="sophiasasaki@gmail.com",  age=15)

QUERY: Retrieve all the users.

User.objects.all()

QUERY: Retrieve the last user.

User.objects.last()

QUERY: Retrieve the first user. 

User.objects.first()

QUERY: Change the user with id=3 so their last name is Pancakes. 

c = User.objects.get(id=3)
c.last_name = "Pancakes"
c.save()

QUERY: Delete the user with id=2 from the database. 

c = User.objects.get(id=2)
c.delete()

QUERY: Get all the users, sorted by their first name. 

User.objects.all().order_by("first_name")

BONUS QUERY: Get all the users, sorted by their first name in descending order.

User.objects.all().order_by("-first_name")